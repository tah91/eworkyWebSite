@model LocalisationQuotationViewModel

@section HeadContent
{
	<title>@Html.WorkiTitle(Worki.Resources.Menu.Menu.Quoations)</title>
	<link href="@Url.VersionedContent(Links.Content.Dashboard_min_css)" rel="stylesheet" type="text/css" />
}

@{Html.RenderAction(MVC.Backoffice.Localisation.HorizontalMenu(Model.Item.ID, LocalisationMainMenu.Home));}

<div class="detailContent">

	<div class="blockContainer float-left contentLeft">
		@{Html.RenderAction(MVC.Backoffice.Localisation.VerticalMenu(Model.Item.ID, (int)LocalisationMenu.Quotations));}
	</div>

	<div class="blockContainer float-left contentRight">
        <div class="contentBlock">
            @Html.Partial(MVC.Backoffice.Shared.Views._QuotationList, new QuotationListViewModel
            {
                Quotations = Model.List,
                Title = Model.Item.Name,
                UrlMaker = x => Url.Action(MVC.Backoffice.Localisation.Quotation(Model.Item.ID, x)),
                EmptyMessage = string.Format(Worki.Resources.Views.BackOffice.BackOfficeString.NoWorkingQuotation, Html.ActionLink(Worki.Resources.Views.BackOffice.BackOfficeString.Offers, MVC.Backoffice.Home.Localisations()))
            })
        </div>
	</div>
	<div class="clear">
	</div>
</div>